<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_product_price_display_pro.ProductScreen" t-inherit="point_of_sale.ProductScreen" t-inherit-mode="extension">
        <xpath expr="//ProductCard" position="replace">
            <ProductCard
                                t-foreach="productsToDisplay" t-as="product" t-key="product.id"
                                productId="product.id"
                                product="product"
                                class="pos.productViewMode"
                                name="getProductName(product)"
                                color="product.color || product.pos_categ_ids?.at(-1)?.color"
                                imageUrl="(pos &amp;&amp; pos.config &amp;&amp; pos.config.show_product_images) ? this.getProductImage(product) : ''"
                                onClick.bind="() => this.addProductToOrder(product)"
                                productInfo="true"
                                productCartQty="this.state.quantityByProductTmplId[product.raw.product_tmpl_id]"
                                onProductInfoClick.bind="() => this.onProductInfoClick(product)"
                                displayPrice="(pos &amp;&amp; pos.config &amp;&amp; pos.config.display_product_price_on_card !== false) ? getProductPrice(product) : ''"
                                priceLabel="(pos &amp;&amp; pos.config &amp;&amp; pos.config.display_product_price_on_card !== false) ? getProductPriceLabel() : ''"/>
        </xpath>
    </t>
</templates>
