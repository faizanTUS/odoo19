<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="crm_lead_view_form_quick_task" model="ir.ui.view">
        <field name="name">crm.lead.form.quick.task</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='lead']" position="after">
                <page string="Tasks" name="tasks_tab">
                    <field name="task_count" invisible="1"/>
                    <field name="quick_task_project_id" invisible="1"/>
                    <div class="mb-3">
                        <button name="action_open_quick_task" string="Quick Task" type="object"
                            class="btn-primary" icon="fa-plus"/>
                    </div>
                    <field name="task_ids" create="0" delete="0" edit="0">
                        <list string="Quick Tasks" default_order="create_date desc">
                            <field name="name" string="Task"/>
                            <field name="project_id"/>
                            <field name="user_ids" widget="many2many_avatar_user"/>
                            <field name="date_deadline"/>
                            <field name="tag_ids" widget="many2many_tags"/>
                            <field name="stage_id"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
