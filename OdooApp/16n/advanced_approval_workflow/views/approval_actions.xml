<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Server Actions -->
    <record id="action_approval_submit" model="ir.actions.server">
        <field name="name">Submit for Approval</field>
        <field name="model_id" ref="model_multi_approval"/>
        <field name="binding_model_id" ref="model_multi_approval"/>
        <field name="binding_view_types">tree</field>
        <field name="state">code</field>
        <field name="code">
if records:
    records.action_submit()
        </field>
    </record>

    <record id="action_approval_approve" model="ir.actions.server">
        <field name="name">Approve</field>
        <field name="model_id" ref="model_multi_approval"/>
        <field name="binding_model_id" ref="model_multi_approval"/>
        <field name="binding_view_types">tree</field>
        <field name="state">code</field>
        <field name="code">
if records:
    records.action_approve()
        </field>
    </record>
</odoo>

