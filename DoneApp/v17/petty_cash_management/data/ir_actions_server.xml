<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_petty_cash_voucher_export_csv" model="ir.actions.server">
        <field name="name">Petty Cash Aging Report (Excel CSV)</field>
        <field name="model_id" ref="model_petty_cash_voucher"/>
        <field name="binding_model_id" ref="model_petty_cash_voucher"/>
        <field name="binding_view_types">tree</field>
        <field name="state">code</field>
        <field name="code">
action = {
    'type': 'ir.actions.act_url',
    'url': '/petty_cash/aging_report_csv?ids=' + ','.join(str(i) for i in records.ids),
    # Use same tab so the browser does not block the download (no "Allow pop-ups" / "Allow once").
    'target': 'self',
}
        </field>
    </record>
</odoo>
