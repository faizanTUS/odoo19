<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">
        <!-- Scheduled task for cleaning up verified OTP records -->
        <record id="ir_cron_cleanup_verified_otp" model="ir.cron">
            <field name="name">Clean Up Verified OTP Records</field>
            <field name="model_id" ref="model_otp_verification"/>
            <field name="state">code</field>
            <field name="code">model._cron_cleanup_verified_otp()</field>
            <field name="interval_number">7</field>
            <field name="interval_type">days</field>
<!--            <field name="numbercall">-1</field>-->
            <field name="active">True</field>
        </record>
    </data>
</odoo>
